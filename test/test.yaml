version: 1.0
application:
  name: "Sample Web App"
  environment: "production"
  debug: false

database:
  host: "localhost"
  port: 5432
  name: "sample_db"
  user: "db_user"
  password: "securepassword"

logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
  what_about_lists:
    - "hello2"
    - "and some other"
  test: |
    hello
    and some other